package com.dl630.isocalc.core.callbacks;

import com.dl630.isocalc.controller.component.ListIsotopeEntryController;
import javafx.event.Event;

public interface ListIsotopeEntryCallback {
    void callback(Event event, ListIsotopeEntryController controller);
}
